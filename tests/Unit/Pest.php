<?php

use Tests\TestCase;

/*
|--------------------------------------------------------------------------
| Unit Test Case
|--------------------------------------------------------------------------
|
| Every unit test in the `tests/Unit` directory is bound to the base
| `TestCase`, giving access to the Laravel application container while
| keeping unit tests focused on isolated classes without HTTP/DB I/O.
|
*/

uses(TestCase::class)->in(__DIR__);
